<!DOCTYPE html>
<html>
  <div class="search">
    <div class="search_fields">
      <p>Search for Bird Sightings by Zip Code</p>
        <div class='zip_field'>
          <%= form_tag zip_location_path, method: :get do %>
            <%= text_field_tag :zip %>
            <%= submit_tag 'Get Data By Zip' %>
          <% end %>
        </div>
      <p>Choose a State from the Dropdown in Order to Find a Specific County</p>
        <div class='state_field'>
          <%= form_tag state_location_path, method: :get do |f| %>
          <%= select_tag :state,  options_for_select(@locations.states) %>
          <%= submit_tag 'Get Counties By State' %>
          <% end %>
        </div>
      <p>Search for Bird Sightings by County</p>
      <div class='county_field'>
        <%= form_tag county_birds_path, method: :get do |f| %>
        <%= select_tag :county,  options_for_select(@locations.state_counties) %>
        <%= submit_tag 'Get Birds By County' %>
        <% end %>
      </div>
    </div>
  </div>
</html>
